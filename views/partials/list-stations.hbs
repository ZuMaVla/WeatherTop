{{#each stations}}
  <div class="box has-text-centered mb">
    {{> summary-reports}}
   <div class="icon-column">
      <p class="mb-6">
        <a href="/station/{{_id}}" class="button is-primary">
          {{>icons/open-station}}
        </a>  
      </p>
      <p>
        <a href="/deletestation/{{_id}}" class="button is-danger">
          {{> icons/delete}}
        </a>  
      </p>
    </div>
  </div>
{{/each}}